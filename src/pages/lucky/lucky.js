// Assuming you have an HTML button with the id "myButton"
const button = document.getElementById("button_p");

// Function to handle button click event
function handleClick() {
  // Replace "https://example.com" with the actual website URL you want to link to
  window.location.href = "https://lucky-manatee-2b2d39.netlify.app/";
}

// Attach the event listener to the button
button.addEventListener("click", handleClick);
